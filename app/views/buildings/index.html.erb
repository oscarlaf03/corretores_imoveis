  <div class="container building-index">
    <h1>Escolha o seu imóvel</h1>
    <div class="row building-card-container">
      <% @buildings.each do |building| %>
        <%= render 'shared/building_card', building: building %>
      <% end %>
    </div>

    <div
      id="map"
      style="width: 100%;
      height: 600px;"
      data-markers="<%= @markers.to_json %>"
    >
    </div>
  </div>
